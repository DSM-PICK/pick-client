import React from 'react';
import * as S from './styles';

const AttendanceNav = (props) => {
	const { classes } = props;

	console.log(classes);

	return (
		<S.Container>
			{/* {
            classes.map(class => 
                    
            )
        } */}
		</S.Container>
	);
};

export default AttendanceNav;
